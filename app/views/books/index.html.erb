<h1>Books</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th colspan="3"></th>
  </tr>

  <%= @books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td>Show</td>
    <td>Edit</td>
    <td>Destroy</td>
  </tr>
  <% end %>
</table>

<h2>New book</h2>
<%= form_with model: Book.new do |f| %>
<% end %>

